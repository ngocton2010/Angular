import { SearchManuFaturePipe } from './search-manu-fature.pipe';

describe('SearchManuFaturePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchManuFaturePipe();
    expect(pipe).toBeTruthy();
  });
});
